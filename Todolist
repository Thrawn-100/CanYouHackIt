<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-Do List Form</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      /* New clear To-Do List themed background image */
      background: url('https://images.unsplash.com/photo-1581090700220-8bb4f857d9bb?auto=format&fit=crop&w=1950&q=80') no-repeat center center fixed;
      background-size: cover;
      margin: 0;
      padding: 0;
    }

    /* Overlay with very low opacity so background is visible */
    .overlay {
      background: rgba(255, 255, 255, 0.05); /* almost transparent */
      min-height: 100vh;
      padding: 50px 0;
    }

    .container {
      max-width: 550px;
      margin: auto;
      background: rgba(255, 255, 255, 0.7); /* semi-transparent for readability */
      backdrop-filter: blur(12px);
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0px 6px 18px rgba(0,0,0,0.3);
    }

    h1 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .form-group {
      margin-bottom: 20px;
      padding: 15px;
      border-radius: 12px;
      color: white;
    }

    .form-group p {
      margin: 0 0 8px;
      font-weight: bold;
      font-size: 15px;
    }

    .inputs {
      display: flex;
      gap: 10px;
    }

    .inputs input {
      flex: 1;
      padding: 10px;
      border-radius: 8px;
      border: none;
      outline: none;
      transition: 0.3s;
    }

    .inputs input[type="text"],
    .inputs input[type="email"] {
      flex: 2;
    }

    .inputs input:focus {
      box-shadow: 0 0 6px rgba(255,255,255,0.8);
    }

    /* Colorful task blocks */
    .work1 { background: linear-gradient(135deg, #667eea, #764ba2); }
    .work2 { background: linear-gradient(135deg, #43cea2, #185a9d); }
    .work3 { background: linear-gradient(135deg, #ff9966, #ff5e62); }
    .work4 { background: linear-gradient(135deg, #36d1dc, #5b86e5); }

    /* Mail section style */
    .mail-section {
      background: linear-gradient(135deg, #f7971e, #ffd200);
      color: #333;
    }

    input[type="submit"] {
      display: block;
      margin: 20px auto 0;
      padding: 12px 25px;
      background: linear-gradient(135deg, #ff512f, #dd2476);
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: bold;
      font-size: 16px;
      transition: 0.3s;
    }

    input[type="submit"]:hover {
      opacity: 0.85;
    }

    @media (max-width: 600px) {
      .inputs { flex-direction: column; }
    }
  </style>
</head>
<body>
  <div class="overlay">
    <div class="container">
      <h1>To-Do List Form</h1>
      <form action="action123.php" method="post">
        <!-- Mail ID Input -->
        <div class="form-group mail-section">
          <p>Mail ID</p>
          <div class="inputs">
            <input type="email" name="mail" placeholder="Enter your email" required>
          </div>
        </div>

        <!-- Work Inputs -->
        <div class="form-group work1">
          <p>Work 1</p>
          <div class="inputs">
            <input type="text" name="work1" placeholder="Enter task here">
            <input type="time" name="time1">
          </div>
        </div>
        <div class="form-group work2">
          <p>Work 2</p>
          <div class="inputs">
            <input type="text" name="work2" placeholder="Enter task here">
            <input type="time" name="time2">
          </div>
        </div>
        <div class="form-group work3">
          <p>Work 3</p>
          <div class="inputs">
            <input type="text" name="work3" placeholder="Enter task here">
            <input type="time" name="time3">
          </div>
        </div>
        <div class="form-group work4">
          <p>Work 4</p>
          <div class="inputs">
            <input type="text" name="work4" placeholder="Enter task here">
            <input type="time" name="time4">
          </div>
        </div>

        <input type="submit" value="Submit">
      </form>
    </div>
  </div>
</body>
</html>
